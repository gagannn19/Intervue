// Shared scheduling option lists. Kept in one place so the old "Custom
// Settings" form (DifficultyDurationSelector) and the new step-by-step
// wizard stay in sync — add a duration here and both flows get it.

export const DIFFICULTIES = ["Easy", "Medium", "Hard"];

// Minutes. Order matters — this is also the on-screen order.
export const DURATIONS = [15, 30, 45, 60];
